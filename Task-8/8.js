// Create three variables and assign numerical values
let num1 = 15;
let num2 = 20;
let num3 = 4;

// Add the values of the first two variables
let sum = num1 + num2;

// Divide the sum by the value of the third variable
let quotient = sum / num3;

// Get the remainder of the division
let remainder = sum % num3;

// Display the remainder
console.log(`The remainder of ${sum} divided by ${num3} is ${remainder}`);